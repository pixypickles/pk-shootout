# PK合戦 Firebase通信試作 α v019

## 修正
- v018で発生した「接続エラー・番号/QRが出ない」不具合を修正
- 原因だった未定義変数 `roomNo` の参照を廃止
- v017の安定した部屋作成処理をベースに戻した
- 一時切断時は部屋全体を削除せず `hostOnline=false` のみ保存
- タブ復帰・画面復帰時に `hostOnline=true` へ戻す
- 不具合を起こした診断コードは削除
